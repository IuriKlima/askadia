import {z} from 'zod';
export const strategyOutputSchema=z.object({positioning:z.string().max(4000),objectives:z.array(z.object({goal:z.string().max(1000),metric:z.string().max(500),suggestedTarget:z.string().max(500)})).min(1).max(6),calendar:z.array(z.object({week:z.number().int().min(1).max(4),format:z.enum(['imagem','carrossel','video']),theme:z.string().max(500),brief:z.string().max(1500),needsClientVideo:z.boolean()})).length(12),ads:z.array(z.object({channel:z.enum(['Meta Ads','Google Ads']),objective:z.string().max(1000),audience:z.string().max(1500),copy:z.string().max(2000),budgetProposal:z.string().max(500),requiredApprovals:z.array(z.string().max(500)).max(8)})).max(5),keywords:z.array(z.string().min(1).max(100)).length(20),unknowns:z.array(z.string().max(1000)).max(20)});
export type GeneratedStrategy=z.infer<typeof strategyOutputSchema>;

/** Editorial themes, not claims about local events, weather or offers. */
export function seasonalContext(month:string){const m=Number(month.slice(5,7));const themes=['Retomada de hábitos e organização da rotina','Consistência e treinos durante viagens e mudanças de rotina','Continuidade das metas do trimestre','Bem-estar e atividade física','Força, autonomia e bem-estar em diferentes fases da vida','Consistência nos meses mais frios, quando aplicável à região','Rotina de férias e atividades em família, se houver serviços adequados','Retomada da rotina e revisão de metas','Primavera e renovação de hábitos','Atividade física, prevenção e orientação profissional','Planejamento da reta final do ano; ofertas somente se confirmadas','Balanço do ano, horários especiais a confirmar e planejamento do próximo ciclo'];return {month,theme:themes[m-1],verifiedDates:m===4?[{date:month+'-07',name:'Dia Mundial da Saúde',source:'https://www.who.int/campaigns/world-health-day'}]:[],rule:'Temas editoriais sugeridos. Não inventar feriados, campanhas promocionais, horários especiais ou eventos locais. Datas móveis e locais precisam ser confirmadas.'};}
export function weekStart(date:string){const d=new Date(date+'T12:00:00Z');d.setUTCDate(d.getUTCDate()-((d.getUTCDay()+6)%7));return d.toISOString().slice(0,10);}
export function respectsWeeklyLimit(dates:string[],existing:string[]=[]){const counts=new Map<string,number>();for(const d of [...dates,...existing]){const key=weekStart(d);counts.set(key,(counts.get(key)??0)+1);}return [...counts.values()].every(n=>n<=2);}
